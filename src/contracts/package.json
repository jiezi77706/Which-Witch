{
  "name": "whichwitch-zeta-contracts",
  "version": "1.0.0",
  "description": "WhichWitch ZetaChain smart contracts deployment",
  "main": "deploy.js",
  "scripts": {
    "deploy:testnet": "node deploy.js deploy zetaTestnet",
    "deploy:mainnet": "node deploy.js deploy zetaMainnet",
    "configure:testnet": "node deploy.js configure zetaTestnet",
    "configure:mainnet": "node deploy.js configure zetaMainnet",
    "compile": "solc --abi --bin --optimize --overwrite -o build/ zeta/ZetaCrossChainPayment.sol",
    "test": "echo 'Tests will be added when Foundry is available'",
    "verify": "echo 'Contract verification script'"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "solc": "^0.8.20"
  },
  "keywords": [
    "ethereum",
    "zetachain",
    "smart-contracts",
    "cross-chain",
    "nft"
  ],
  "author": "WhichWitch Team",
  "license": "MIT"
}